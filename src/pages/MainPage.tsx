import { SideBar } from "../features/SideBar/SideBar";
import { Map } from "../features/Map/Map";

export const MainPage = () => {
    return (
        <div>
            <SideBar/>
            <Map/>
        </div>
    )
}